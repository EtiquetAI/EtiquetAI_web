{
  "name": "qr-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npx lite-server --baseDir=./"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^5.0.0",
    "esbuild": "^0.19.0"
  }
}
